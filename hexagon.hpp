// #pragma once
// #include <vector>

// #include "edge.hpp"
// #include "vertex.hpp"

// class Hexagon {
//    public:
//     int id;               // Every hexagon has a number
//     Edge* hexEdge[6];      // Each hexagon has 6 edges
//     Vertex* hexVertex[6];  // Each hexagon has 6 vertex

//     Hexagon(int hexagonId, Edge hexEdgeArr[6], Vertex hexVertexArr[6]) : id(hexagonId),{
//     for (int i = 0; i < 6; i++) {
//         hexEdge[i] = hexEdgeArr[i];
//         hexVertex[i] = hexVertexArr[i];
//     }
// }
// };